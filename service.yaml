apiVersion: v1
kind: Service
metadata:
  name: register-service
spec:
  selector:
    app: register
  type: NodePort
  ports:
    - protocol: TCP
      port: 5000        # Service port
      targetPort: 5000  # Container port matches Flask
      nodePort: 30085    # Access from outside cluster
